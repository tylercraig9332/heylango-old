interface WordLearnerProps {
    value?: string,
    onChange?: any,
    readOnly?: boolean,
    style?: React.CSSProperties,
    wordsPerPage?: number,
    fontSize?: string,
    lineHeight?: string
}

export default WordLearnerProps